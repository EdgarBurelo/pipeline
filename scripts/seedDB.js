// DROP DATABASE IF EXISTS CRM;

// CREATE DATABASE CRM;

// USE CRM;

// CREATE TABLE users(
// name VARCHAR(60), 
// email VARCHAR(30), 
// profile INT, 
// password VARCHAR(30), 
// createdAt TIMESTAMP, 
// updatedAt TIMESTAMP, 
// company VARCHAR(60)
// );

// INSERT INTO users(name, email, profile, password, createdAt, updatedAt, company)
// VALUES("César Gutiérrez", "hola@mail.com", 1, "alskdjf", '2011-03-12', '2011-03-12', "company");
// SELECT * FROM users;